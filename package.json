{
  "name": "wasm-mandelbrot",
  "version": "1.0.0",
  "description": "Mandelbrot set viewer implemented in Rust",
  "main": "index.js",
  "scripts": {
    "build-wasm" : "wasm-pack build --target web",
    "build-bundle":"webpack",
    "cp-html": "cp index.html dist/index.html",
    "build":"npm run build-wasm && npm run build-bundle && npm run cp-html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rimraf": "^5.0.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  }
}
